const axios = require('axios');

const {{repo}} = async (search) => {
    const data = axios.get({{dataSource}}, { validateStatus: false });
    return data;
}

module.exports = {
    fetchData: axios,
    {{repo}}
};