const axios = require('axios');

const {{route}}Repository = async ({{args}}) => {
    const data = axios.get({{dataSource}}, { validateStatus: false });
    return data;
}

module.exports = {
    fetchData: axios,
    {{route}}Repository
};