const repo = require('../repositories/{{route}}');
const model = require('../models/{{route}}');

const {{route}} = async ({{inputs}}) => {
    const data = await repo({{inputs}});
    return model(data);
}

module.exports = {{route}};