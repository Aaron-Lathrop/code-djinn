const repo = require('../repositories/{{route}}Repository');
const model = require('../models/{{route}}Model');

const {{route}}DataService = async ({{inputs}}) => {
    const data = await repo.{{route}}Repository({{inputs}});
    return model.{{route}}Model(data);
}

module.exports = {
    repo,
    model,
    {{route}}DataService
}