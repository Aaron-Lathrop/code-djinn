const repo = require('../repositories/{{route}}Repository');
const model = require('../models/{{route}}Model');

const {{route}}DataService = async ({{args}}) => {
    const data = await repo.{{route}}Repository({{args}});
    return model.{{route}}Model(data);
}

module.exports = {
    repo,
    model,
    {{route}}DataService
}